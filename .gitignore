# Persona/knob store files (per-tenant variants too) — local, hand-inspectable state
.oidc-personas*.json

# Fresh build output (only created if you ever run a build)
_fresh/
vite.config.ts.timestamp-*

# Harness browser bundle (regenerate with: deno task bundle)
harness/dist/

# Deno
.deno/

# OS / editor
.DS_Store
*.swp
